name: 'portfolio_ecomm'
version: '1.0.0'

profile: 'portfolio_ecomm'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  portfolio_ecomm:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
